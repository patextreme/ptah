## 1. Structured runtime facts

- [x] 1.1 Add `SessionEvent::SessionReady { label, agent, command, args, env_keys, acp_id }` to `crates/ptah-core/src/events.rs`, with a doc comment stating that sinks read readiness facts from this event, not from rendered wording. Verify: `cargo test -p ptah-core` passes and a new unit test constructs the variant carrying all fields.
- [x] 1.2 Add `Registry::raw(&self, name: &str) -> Option<&AgentSpec>` to `crates/ptah-core/src/config/mod.rs` as the pre-interpolation view, leaving `resolve`/`resolve_with` interpolating. Verify: a unit test asserts the accessor returns the authored `${VAR}` text while `resolve` returns the interpolated value.
- [x] 1.3 Capture the authored spec for inline specs and emit `SessionReady` from `crates/ptah-luau/src/bindings.rs` after `start_session` returns: label, agent name (registry name for named agents, authored command for inline specs), authored `command`/`args`/env key names, and the ACP id from the returned handle. Inline specs pass their authored table values through `new_agent_factory` before interpolation. Verify: `cargo test -p ptah-luau` passes and a new test asserts the emitted event carries the ACP id and the unresolved `command`/`args`.
- [x] 1.4 Remove the rendered `session ready` lifecycle emission from `crates/ptah-acp/src/driver.rs` (the `ready_tx` send stays). Verify: `cargo test -p ptah-acp` passes.
- [x] 1.5 Render the ready line from `SessionReady` in `crates/ptah-render/src/lib.rs` (verbose-only, byte-identical `{label}: session ready (acp {acp_id})`) and add an arm that otherwise ignores the variant. Verify: `cargo test -p ptah-render` passes, and the existing verbose ready-line assertion in `crates/ptah-cli/tests/cli.rs` (`session_ready_line_carries_the_acp_session_id`) still passes unedited.
- [x] 1.6 Add `cancelled: bool` to `RunOutcome` (`crates/ptah-luau/src/state.rs`) and set it `true` on every cancel return in `crates/ptah-luau/src/run.rs` (the `End::Cancelled` return and each `shutdown_*` return), `false` on the rest, so a signal is distinguishable from a script's own `ptah.exit(130/143)`. Verify: `cargo test -p ptah-luau` passes and a unit test drives `run` with a pre-fired `shutdown` watch, asserting the flag is set only on the cancel path.

## 2. The record module in `ptah-render`

- [x] 2.1 Add `getrandom = "0.4"` to `[workspace.dependencies]` in the root `Cargo.toml` (the `0.4.3` entry is already resolved in `Cargo.lock` via `pesde`'s `gix`/`tempfile`/`uuid` stack) and declare `serde`, `serde_json`, and `getrandom` in `crates/ptah-render/Cargo.toml` via the workspace table. Verify: `cargo build -p ptah-render` succeeds and `Cargo.lock` gains no new crate.
- [x] 2.2 Implement run-id minting: the start instant in UTC as `yyyymmddhhmmss`, `-`, and a short decimal suffix from `getrandom`; re-mint while the target directory exists. Verify: unit tests assert the shape, that distinct-second ids sort in start order (same-second suffix order is not asserted), a UTC encoding under a non-UTC local zone, and that an occupied id yields a different one.
- [x] 2.3 Implement project-root resolution: nearest ancestor containing `.ptah/`, else the invocation directory. Verify: unit tests over tempdirs cover both branches and the case where an intermediate directory has no `.ptah`.
- [x] 2.4 Implement record creation: create the directory and `.gitignore` containing `*` as one unit, leave an existing ignore file unmodified, and leave no directory behind when either step fails. Verify: unit tests cover first-run creation, preservation of a user-authored ignore file, and an unwritable location leaving no directory.
- [x] 2.5 Implement the log writer: a `Renderer` over the record's `log` file built from the terminal's verbosity with `quiet: false, no_color: true`. Verify: unit tests assert a record renderer still writes lines the terminal suppressed, and that its output carries no ANSI escapes.
- [x] 2.6 Implement the fan-out sink: an `EventSink` that forwards `emit` and `script_log` to each inner sink in order. Verify: a unit test with two recording sinks asserts identical, equally ordered sequences for both.
- [x] 2.7 Implement the `run.json` model: schema version, run id, script path, argv, invocation directory, ptah version, start/end as RFC 3339 UTC, status, exit code, error, sessions (label, agent, pre-interpolation `command`/`args`, env key names, ACP session id, populated from `SessionReady`), and asks (ordinal, prompt, details, action, text), with `status` derived from `RunOutcome`'s cancellation flag and exit code as `ok`/`failed`/`cancelled`/`running`. Verify: unit tests round-trip a fully populated record, assert a non-zero explicit exit maps to `failed`, and assert the JSON is indented and newline-terminated.
- [x] 2.8 Implement atomic rewriting (a sibling temp file renamed over `run.json`) behind an accessor the composition root drives. Verify: a unit test that interrupts a write asserts the previous file is still intact and parses.
- [x] 2.9 Implement the secrets rule: populate each session's `command`/`args` from `SessionReady`'s authored values (read at the emission site through `Registry::raw` or the authored inline table) and record env key names only, never values. Verify: a unit test with an interpolated arg and an env table asserts the value appears nowhere in the serialized record, and an inline-spec test asserts the authored values round-trip.

## 3. Composition root wiring

- [x] 3.1 In `crates/ptah-cli/src/cli.rs`, mint the run id and create the record before the renderer is constructed, build the terminal and record renderers behind the fan-out, keep the fan-out the value handed to the runtime as `Arc<dyn EventSink>`. Verify: a new integration test in `crates/ptah-cli/tests/` finds `.ptah/runs/<id>/log` and `run.json` after a run against the mock agent.
- [x] 3.2 Drive the rewrite triggers — run start, `SessionReady`, ask requested, ask resolved, and every terminal transition including signal teardown — so each fact lands in `run.json` as it is learned. Derive the final `status` from `RunOutcome`'s cancellation flag (1.6), never from the exit code alone, so a signal is `cancelled` while `ptah.exit(130)` is `failed`. Assign ask ordinals from the record's own emission-order count of `AskRequested` (asks are serialized in issue order, so this matches the rendered `ask {n}`). Verify: integration tests assert `run.json` after a run names its sessions with ACP ids and authored shapes, that a second ask is recorded with ordinal 2 and its resolution, that a first SIGINT records `cancelled` with no resolution for a dropped ask, and that a script calling `ptah.exit(130)` records `failed` with exit code 130.
- [x] 3.3 Implement the best-effort failure path: one warning to stderr naming the reason and location, printed even under `--quiet`, with the run's exit code and behavior otherwise untouched; disable the record and warn once on a mid-run write failure. Verify: an integration test with an unwritable `.ptah` asserts the script still runs, exactly one warning prints, and the exit code is the script's own.
- [x] 3.4 Ensure no record is created for a pre-flight failure, and that `check`, `types`, `init`, and `package` create none. Verify: integration assertions for each command.

## 4. The run-start line

- [x] 4.1 Render one `ptah`-attributed line at run start naming the record directory, using the established path rule (relative to the invocation directory, `~` under home, otherwise as received) and the `exec_line` gating (present in every non-quiet mode, suppressed on the terminal by `--quiet`, always written to the record). The composition root keeps both `Arc<Renderer>` handles (terminal and record) and calls the line on each after the record exists, before handing the fan-out to the runtime; when the record cannot be created, the failure warning replaces it. Verify: a `ptah-render` unit test for the path forms, and an integration test asserting the line at default verbosity, its absence from the terminal under `--quiet`, and its presence in `log`.

## 5. Documentation

- [x] 5.1 Add a README section describing `.ptah/runs/`: the layout, that `--quiet` suppresses the terminal and not the record, that the record is not a stdout capture (so `print` is absent and `ptah.log` is the script-side route), and a security note naming `run.json` as the most secret-bearing file ptah writes. Verify: read-through against the specs, with no contradiction between README and the `run-record` requirements.
- [x] 5.2 Update `skills/ptah/SKILL.md` (~lines 440-447): drop the instruction to hand-embed `session:sessionId()` into `ptah.ask` details for correlation, pointing at the run record instead, while keeping the `sessionId()` method rows for scripts that genuinely use the id. Verify: `grep -n sessionId skills/ptah/SKILL.md` shows no remaining hand-embedding instruction and the method documentation is intact.

## 6. Gates

- [x] 6.1 Run the full gates and confirm nothing regressed: `cargo test` for the whole workspace, `stylua .` (no changes expected — no `.luau` file is edited), `ptah check` over the repo's bundled scripts, and `nix flake check`. Verify: all pass, and `crates/ptah-cli/tests/deps_guard.rs` still passes with core's I/O-freedom intact (the record adapter lives in `ptah-render`).
